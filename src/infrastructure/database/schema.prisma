generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}


model Tache {
  id String @id @default(uuid())
  title String
  description String?
  priorite Int
}